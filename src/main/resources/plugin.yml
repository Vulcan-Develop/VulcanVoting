name: ${project.artifactId}
main: ${project.groupId}.${project.artifactId}
version: ${project.version}
authors: [OfficialGaming]
depend:
  - Votifier
commands:
  fakevote:
    description: process fake vote
    aliases: [vulcanfakevote]
  vote:
    description: vote cmd
    aliases: [vulcanvote]
